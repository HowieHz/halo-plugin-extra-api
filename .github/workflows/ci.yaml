name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened

jobs:
  required-checks:
    name: Required Checks
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout Source
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # https://github.com/actions/checkout/tree/v7.0.0
        with:
          fetch-depth: 0
          submodules: true

      - name: Set up JDK
        uses: actions/setup-java@0f481fcb613427c0f801b606911222b5b6f3083a # https://github.com/actions/setup-java/tree/v5
        with:
          distribution: temurin
          java-version: 21

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@3f131e8634966bd73d06cc69884922b02e6faf92 # https://github.com/gradle/actions/tree/v6
        with:
          cache-read-only: false

      - name: Install pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # https://github.com/pnpm/action-setup/releases/tag/v6.0.9
        with:
          version: 11.11.0
          run_install: false

      - name: Resolve pnpm cache dependency paths
        id: pnpm-cache-paths
        shell: bash
        run: |
          {
            echo 'paths<<EOF'
            echo 'ui/pnpm-lock.yaml'
            echo 'js-modules/shiki/pnpm-lock.yaml'
            echo 'EOF'
          } >> "$GITHUB_OUTPUT"

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # https://github.com/actions/setup-node/tree/v6
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: ${{ steps.pnpm-cache-paths.outputs.paths }}

      - name: Check release constraints
        if: github.event_name == 'pull_request'
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          IS_RELEASE_PR: ${{ contains(github.event.pull_request.labels.*.name, 'release') }}
        run: node .github/scripts/check-release-constraints.js

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build and test all variants
        run: ./gradlew test buildAll --console=plain

      - name: Upload CI build artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # https://github.com/actions/upload-artifact/tree/v7
        with:
          name: ci-build-artifacts
          path: build/libs/*.jar
